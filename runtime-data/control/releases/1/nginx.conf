# Generated by PQC Gateway Framework v3.2. Do not edit.
worker_processes auto;
pid /tmp/nginx.pid;

events {
    worker_connections 16384;
    multi_accept on;
}

http {
    include /opt/nginx/conf/mime.types;
    default_type application/octet-stream;
    log_format pq_access escape=json
        '{'
        '"ts":"$time_iso8601",'
        '"protocol_type":"http",'
        '"application_protocol":"$pq_application_protocol",'
        '"service":"$pq_service_name",'
        '"configured_groups":"$pq_configured_groups",'
        '"server_name":"$server_name",'
        '"server_port":$server_port,'
        '"remote_addr":"$remote_addr",'
        '"request_id":"$request_id",'
        '"method":"$request_method",'
        '"uri":"$request_uri",'
        '"status":$status,'
        '"bytes_sent":$bytes_sent,'
        '"request_time":$request_time,'
        '"upstream_addr":"$upstream_addr",'
        '"upstream_status":"$upstream_status",'
        '"upstream_response_time":"$upstream_response_time",'
        '"ssl_protocol":"$ssl_protocol",'
        '"ssl_cipher":"$ssl_cipher",'
        '"ssl_curve":"$ssl_curve",'
        '"ssl_server_name":"$ssl_server_name",'
        '"client_verify":"$ssl_client_verify"'
        '}';
    access_log /var/log/nginx/access.log pq_access;
    error_log /var/log/nginx/error.log info;
    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65s;
    server_tokens off;
    resolver 127.0.0.11 valid=30s ipv6=off;

    server {
        listen 127.0.0.1:18081;
        server_name _;
        location = /nginx_status {
            stub_status;
            access_log off;
        }
    }

    server {
        listen 8443 ssl;
        server_name bank-gateway.local;

        set $pq_service_name "compatibility-gateway";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name off;
        proxy_ssl_verify off;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "compatibility-gateway" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"compatibility-gateway","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "compatibility-gateway";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass http://bank-backend:8080;
        }
    }

    server {
        listen 9443 ssl;
        server_name strict-gateway.local;

        set $pq_service_name "strict-pqc-gateway";
        set $pq_configured_groups "X25519MLKEM768";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name off;
        proxy_ssl_verify off;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "strict-pqc-gateway" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"strict-pqc-gateway","adapter":"http","tls_groups":"X25519MLKEM768"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "strict-pqc-gateway";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass http://bank-backend:8080;
        }
    }

    server {
        listen 10443 ssl;
        server_name mtls-gateway.local;

        set $pq_service_name "mtls-required-gateway";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        ssl_client_certificate /etc/pq-gateway/certs/ca.crt;
        ssl_verify_client on;
        ssl_verify_depth 3;

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name off;
        proxy_ssl_verify off;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "mtls-required-gateway" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"mtls-required-gateway","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "mtls-required-gateway";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass http://bank-backend:8080;
        }
    }

    server {
        listen 11443 ssl;
        server_name optional-mtls-gateway.local;

        set $pq_service_name "mtls-optional-gateway";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        ssl_client_certificate /etc/pq-gateway/certs/ca.crt;
        ssl_verify_client optional;
        ssl_verify_depth 3;

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name off;
        proxy_ssl_verify off;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "mtls-optional-gateway" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"mtls-optional-gateway","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "mtls-optional-gateway";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass http://bank-backend:8080;
        }
    }

    server {
        listen 12443 ssl;
        server_name upstream-gateway.local;

        set $pq_service_name "verified-upstream-mtls";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name on;
        proxy_ssl_name upstream-secure.local;
        proxy_ssl_verify on;
        proxy_ssl_trusted_certificate /etc/pq-gateway/certs/upstream/ca.crt;
        proxy_ssl_verify_depth 3;
        proxy_ssl_certificate /etc/pq-gateway/certs/upstream/client.crt;
        proxy_ssl_certificate_key /etc/pq-gateway/certs/upstream/client.key;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "verified-upstream-mtls" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"verified-upstream-mtls","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "verified-upstream-mtls";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass https://secure-backend:8443;
        }
    }

    server {
        listen 13443 ssl;
        server_name badca-gateway.local;

        set $pq_service_name "wrong-ca-upstream-test";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name on;
        proxy_ssl_name upstream-secure.local;
        proxy_ssl_verify on;
        proxy_ssl_trusted_certificate /etc/pq-gateway/certs/wrong-upstream-ca.crt;
        proxy_ssl_verify_depth 3;
        proxy_ssl_certificate /etc/pq-gateway/certs/upstream/client.crt;
        proxy_ssl_certificate_key /etc/pq-gateway/certs/upstream/client.key;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "wrong-ca-upstream-test" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"wrong-ca-upstream-test","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "wrong-ca-upstream-test";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass https://secure-backend:8443;
        }
    }

    server {
        listen 14443 ssl;
        server_name upstream-noclient-gateway.local;

        set $pq_service_name "missing-upstream-client-cert-test";
        set $pq_configured_groups "X25519MLKEM768:X25519";
        set $pq_application_protocol "http";

        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQHTTP:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_ssl_protocols TLSv1.2 TLSv1.3;
        proxy_ssl_server_name on;
        proxy_ssl_name upstream-secure.local;
        proxy_ssl_verify on;
        proxy_ssl_trusted_certificate /etc/pq-gateway/certs/upstream/ca.crt;
        proxy_ssl_verify_depth 3;

        add_header X-PQ-Gateway "pqc-migration-gateway-v3.2" always;
        add_header X-PQ-Service "missing-upstream-client-cert-test" always;
        add_header X-PQ-TLS-Groups "X25519MLKEM768:X25519" always;

        location = /healthz {
            access_log off;
            default_type application/json;
            return 200 '{"status":"ok","service":"missing-upstream-client-cert-test","adapter":"http","tls_groups":"X25519MLKEM768:X25519"}\n';
        }

        location / {
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Request-ID $request_id;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Client-Verify $ssl_client_verify;
            proxy_set_header X-Forwarded-Client-DN $ssl_client_s_dn;
            proxy_set_header X-PQ-TLS-Protocol $ssl_protocol;
            proxy_set_header X-PQ-TLS-Cipher $ssl_cipher;
            proxy_set_header X-PQ-TLS-Group $ssl_curve;
            proxy_set_header X-PQ-Service "missing-upstream-client-cert-test";
            proxy_connect_timeout 5s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
            proxy_pass https://secure-backend:8443;
        }
    }

}

stream {
    resolver 127.0.0.11 valid=30s ipv6=off;
    log_format pq_stream_mqtt_tls_gateway escape=json
        '{'
        '"ts":"$time_iso8601",'
        '"protocol_type":"stream",'
        '"application_protocol":"mqtt",'
        '"service":"mqtt-tls-gateway",'
        '"configured_groups":"X25519MLKEM768:X25519",'
        '"server_name":"mqtt-gateway.local",'
        '"server_port":$server_port,'
        '"remote_addr":"$remote_addr",'
        '"status":$status,'
        '"bytes_sent":$bytes_sent,'
        '"bytes_received":$bytes_received,'
        '"session_time":$session_time,'
        '"upstream_addr":"$upstream_addr",'
        '"ssl_protocol":"$ssl_protocol",'
        '"ssl_cipher":"$ssl_cipher",'
        '"ssl_curve":"$ssl_curve",'
        '"ssl_server_name":"$ssl_server_name",'
        '"client_verify":"$ssl_client_verify"'
        '}';
    log_format pq_stream_tcp_echo_tls_gateway escape=json
        '{'
        '"ts":"$time_iso8601",'
        '"protocol_type":"stream",'
        '"application_protocol":"tcp",'
        '"service":"tcp-echo-tls-gateway",'
        '"configured_groups":"X25519MLKEM768:X25519",'
        '"server_name":"tcp-gateway.local",'
        '"server_port":$server_port,'
        '"remote_addr":"$remote_addr",'
        '"status":$status,'
        '"bytes_sent":$bytes_sent,'
        '"bytes_received":$bytes_received,'
        '"session_time":$session_time,'
        '"upstream_addr":"$upstream_addr",'
        '"ssl_protocol":"$ssl_protocol",'
        '"ssl_cipher":"$ssl_cipher",'
        '"ssl_curve":"$ssl_curve",'
        '"ssl_server_name":"$ssl_server_name",'
        '"client_verify":"$ssl_client_verify"'
        '}';
    log_format pq_stream_legacy_line_tls_gateway escape=json
        '{'
        '"ts":"$time_iso8601",'
        '"protocol_type":"stream",'
        '"application_protocol":"legacy-line",'
        '"service":"legacy-line-tls-gateway",'
        '"configured_groups":"X25519MLKEM768:X25519",'
        '"server_name":"legacy-gateway.local",'
        '"server_port":$server_port,'
        '"remote_addr":"$remote_addr",'
        '"status":$status,'
        '"bytes_sent":$bytes_sent,'
        '"bytes_received":$bytes_received,'
        '"session_time":$session_time,'
        '"upstream_addr":"$upstream_addr",'
        '"ssl_protocol":"$ssl_protocol",'
        '"ssl_cipher":"$ssl_cipher",'
        '"ssl_curve":"$ssl_curve",'
        '"ssl_server_name":"$ssl_server_name",'
        '"client_verify":"$ssl_client_verify"'
        '}';


    server {
        listen 8883 ssl;
        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQSTREAM:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_connect_timeout 5s;
        proxy_timeout 60s;
        proxy_ssl off;
        access_log /var/log/nginx/stream-access.log pq_stream_mqtt_tls_gateway;
        proxy_pass mqtt-broker:1883;
    }

    server {
        listen 15443 ssl;
        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQSTREAM:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_connect_timeout 5s;
        proxy_timeout 60s;
        proxy_ssl off;
        access_log /var/log/nginx/stream-access.log pq_stream_tcp_echo_tls_gateway;
        proxy_pass tcp-backend:9000;
    }

    server {
        listen 16443 ssl;
        ssl_protocols TLSv1.3;
        ssl_certificate /etc/pq-gateway/certs/server.crt;
        ssl_certificate_key /etc/pq-gateway/certs/server.key;
        ssl_conf_command Groups X25519MLKEM768:X25519;
        ssl_session_timeout 10m;
        ssl_session_cache shared:PQSTREAM:50m;
        ssl_session_tickets off;

        # client certificate authentication disabled

        proxy_connect_timeout 5s;
        proxy_timeout 60s;
        proxy_ssl off;
        access_log /var/log/nginx/stream-access.log pq_stream_legacy_line_tls_gateway;
        proxy_pass legacy-backend:9100;
    }

}
